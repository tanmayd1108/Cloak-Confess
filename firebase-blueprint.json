{
  "entities": {
    "Confession": {
      "title": "Confession",
      "description": "An anonymous confession post.",
      "type": "object",
      "properties": {
        "content": { "type": "string", "description": "The text content of the confession." },
        "category": { "type": "string", "description": "The category of the confession (e.g., 'Love', 'Work', 'Life')." },
        "mood": { "type": "string", "enum": ["sad", "love", "drama", "funny"], "description": "The mood of the confession." },
        "tags": { "type": "array", "items": { "type": "string" }, "description": "Keywords for matching." },
        "status": { "type": "string", "enum": ["published", "flagged", "hidden"], "description": "The moderation status." },
        "reportsCount": { "type": "integer", "description": "The number of reports received." },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the confession was posted." },
        "anonymousId": { "type": "string", "description": "A random ID for the poster." }
      },
      "required": ["content", "category", "mood", "tags", "status", "reportsCount", "createdAt", "anonymousId"]
    },
    "Report": {
      "title": "Report",
      "description": "A report on a confession.",
      "type": "object",
      "properties": {
        "confessionId": { "type": "string", "description": "The ID of the reported confession." },
        "reason": { "type": "string", "description": "The reason for reporting." },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the report was made." }
      },
      "required": ["confessionId", "reason", "createdAt"]
    },
    "UserProfile": {
      "title": "UserProfile",
      "description": "User profile information.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "username": { "type": "string" },
        "username_lower": { "type": "string" },
        "avatarUrl": { "type": "string" },
        "bio": { "type": "string" },
        "isPublic": { "type": "boolean" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "streakCount": { "type": "integer" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "username", "username_lower", "avatarUrl", "isPublic", "role", "streakCount", "createdAt"]
    },
    "Username": {
      "title": "Username",
      "description": "Mapping of username to uid for uniqueness.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" }
      },
      "required": ["uid"]
    },
    "SavedConfession": {
      "title": "SavedConfession",
      "description": "A bookmarked confession.",
      "type": "object",
      "properties": {
        "confessionId": { "type": "string" },
        "uid": { "type": "string" },
        "savedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["confessionId", "uid", "savedAt"]
    },
    "Comment": {
      "title": "Comment",
      "description": "A comment on a confession.",
      "type": "object",
      "properties": {
        "confessionId": { "type": "string" },
        "parentCommentId": { "type": "string" },
        "content": { "type": "string" },
        "authorUid": { "type": "string" },
        "anonymousId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["confessionId", "content", "authorUid", "anonymousId", "createdAt"]
    },
    "Reaction": {
      "title": "Reaction",
      "description": "A user's reaction to a confession.",
      "type": "object",
      "properties": {
        "confessionId": { "type": "string" },
        "userId": { "type": "string" },
        "type": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["confessionId", "userId", "type", "createdAt"]
    },
    "DailyPrompt": {
      "title": "DailyPrompt",
      "description": "A daily prompt for confessions.",
      "type": "object",
      "properties": {
        "question": { "type": "string" },
        "category": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["question", "category", "createdAt"]
    },
    "PrivateMessage": {
      "title": "PrivateMessage",
      "description": "A private message in anonymous chat.",
      "type": "object",
      "properties": {
        "chatId": { "type": "string" },
        "senderUid": { "type": "string" },
        "receiverUid": { "type": "string" },
        "content": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["chatId", "senderUid", "receiverUid", "content", "createdAt"]
    }
  },
  "firestore": {
    "/confessions/{confessionId}": {
      "schema": "Confession",
      "description": "Collection of anonymous confessions."
    },
    "/reports/{reportId}": {
      "schema": "Report",
      "description": "Collection of reports on confessions."
    },
    "/users/{uid}": {
      "schema": "UserProfile",
      "description": "Collection of user profiles."
    },
    "/usernames/{username}": {
      "schema": "Username",
      "description": "Collection for unique username mapping."
    },
    "/saved_confessions/{savedId}": {
      "schema": "SavedConfession",
      "description": "Collection of bookmarked confessions."
    },
    "/comments/{commentId}": {
      "schema": "Comment",
      "description": "Collection of comments on confessions."
    },
    "/confession_reactions/{reactionId}": {
      "schema": "Reaction",
      "description": "Collection of user reactions."
    },
    "/daily_prompts/{promptId}": {
      "schema": "DailyPrompt",
      "description": "Collection of daily prompts."
    },
    "/private_messages/{messageId}": {
      "schema": "PrivateMessage",
      "description": "Collection of private messages."
    }
  }
}
